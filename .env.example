# Variables d'environnement pour l'application d'invitation anniversaire

# =============== BASE DE DONNÉES ===============
MONGODB_URI=mongodb://localhost:27017/birthday-invitation

# =============== AUTHENTIFICATION ADMIN ===============
# Hash bcrypt du mot de passe admin (généré avec bcrypt.hash('votre_mot_de_passe', 12))
ADMIN_PASSWORD_HASH=

# Secrets JWT (générer avec crypto.randomBytes(64).toString('hex'))
JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=

# =============== SERVEUR ===============
NODE_ENV=development
PORT=5000
BASE_URL=http://localhost:5000

# URL du frontend (pour CORS)
FRONTEND_URL=http://localhost:3000

# =============== FIREBASE (OPTIONNEL) ===============
# Configuration Firebase pour le stockage des photos
FIREBASE_API_KEY=
FIREBASE_AUTH_DOMAIN=
FIREBASE_PROJECT_ID=
FIREBASE_STORAGE_BUCKET=
FIREBASE_MESSAGING_SENDER_ID=
FIREBASE_APP_ID=

# =============== INSTRUCTIONS ===============
# 1. Copiez ce fichier vers .env
# 2. Remplissez toutes les valeurs nécessaires
# 3. Générez un hash de mot de passe avec : node -e "console.log(require('bcryptjs').hashSync('VOTRE_MOT_DE_PASSE', 12))"
# 4. Générez les secrets JWT avec : node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# 5. Configurez Firebase si vous voulez utiliser le stockage de photos